program prog11 is
    x, y : integer ; #Incorrect symbol '#' instead of ':'.
begin
    write "Enter 2 numbers: ";
    read x, y;
    write "Their sum is: ";
    z := x + y; #Using 'z' witout declaraion.
    writeln int2string(z);
end